# Руководство по внесению вклада в NeuroScraper Pro

Спасибо за интерес к улучшению проекта! Мы приветствуем любой вклад.

## Как внести вклад

### Сообщение об ошибках

Если вы нашли баг:

1. Убедитесь, что баг еще не был зарепортен
2. Создайте Issue с подробным описанием:
   - Шаги для воспроизведения
   - Ожидаемое поведение
   - Фактическое поведение
   - Версия Python и ОС
   - Логи из `bot.log`

### Предложение функционала

Есть идея для новой функции?

1. Проверьте, нет ли уже подобного предложения
2. Опишите:
   - Зачем это нужно
   - Как это должно работать
   - Примеры использования

### Pull Requests

#### Процесс

1. Fork репозитория
2. Создайте ветку для вашей фичи (`git checkout -b feature/amazing-feature`)
3. Внесите изменения
4. Проверьте код:
   ```bash
   # Форматирование
   black .

   # Линтинг
   flake8 .

   # Типы
   mypy .
   ```
5. Commit изменений (`git commit -m 'Add amazing feature'`)
6. Push в ветку (`git push origin feature/amazing-feature`)
7. Откройте Pull Request

#### Требования к коду

- Следуйте PEP 8
- Документируйте функции (docstrings)
- Добавляйте типы где возможно
- Пишите осмысленные commit messages
- Обновляйте CHANGELOG.md

### Структура commit message

```
<type>: <short summary>

<detailed description>

<footer>
```

**Types:**
- `feat`: Новая функция
- `fix`: Исправление бага
- `docs`: Документация
- `style`: Форматирование
- `refactor`: Рефакторинг
- `test`: Тесты
- `chore`: Рутинные задачи

**Пример:**
```
feat: Add CSV export functionality

- Implemented CSV export in excel_generator.py
- Added new button in keyboards.py
- Updated user_handlers.py to handle CSV export

Closes #42
```

## Стиль кода

### Python Style Guide

```python
# Хорошо
def parse_channel_comments(
    session_name: str,
    channel_link: str,
    time_filter_days: Optional[int] = None
) -> ParsingResult:
    """
    Parse comments from a Telegram channel.

    Args:
        session_name: Name of the Telethon session
        channel_link: Link to the channel
        time_filter_days: Filter by activity days

    Returns:
        ParsingResult with users and admins
    """
    pass

# Плохо
def parse(s, c, t=None):
    pass
```

### Именование

- **Модули**: `lowercase_with_underscores.py`
- **Классы**: `PascalCase`
- **Функции**: `lowercase_with_underscores`
- **Константы**: `UPPERCASE_WITH_UNDERSCORES`
- **Приватные**: `_leading_underscore`

### Документация

```python
class ExcelGenerator:
    """
    Генератор Excel и TXT отчетов.

    Attributes:
        output_dir: Директория для сохранения отчетов
    """

    def generate_reports(
        self,
        result: ParsingResult,
        parse_type: str
    ) -> tuple[Optional[Path], Optional[Path]]:
        """
        Создать оба отчета: Excel и TXT.

        Args:
            result: Результат парсинга
            parse_type: Тип парсинга ('channel' или 'chat')

        Returns:
            Tuple of (excel_path, txt_path)

        Raises:
            IOError: Если не удалось создать файлы
        """
        pass
```

## Тестирование

### Запуск тестов

```bash
# Все тесты
pytest

# С покрытием
pytest --cov=. --cov-report=html

# Конкретный файл
pytest tests/test_database.py
```

### Написание тестов

```python
import pytest
from database import Database

@pytest.mark.asyncio
async def test_create_user():
    """Test user creation"""
    db = Database(":memory:")
    await db.init_db()

    result = await db.create_user(
        user_id=123,
        username="testuser"
    )

    assert result is True
    user = await db.get_user(123)
    assert user["username"] == "testuser"
```

## Приоритеты разработки

### Высокий приоритет
- Исправление критических багов
- Улучшение безопасности
- Оптимизация производительности

### Средний приоритет
- Новые функции из roadmap
- Улучшение UX/UI
- Расширение документации

### Низкий приоритет
- Косметические изменения
- Экспериментальные функции

## Архитектура проекта

### Основные компоненты

```
┌─────────────┐
│   main.py   │  <- Entry point
└──────┬──────┘
       │
       ├─> handlers/        <- User interactions
       │   ├─> user_handlers.py
       │   └─> admin_handlers.py
       │
       ├─> services/        <- Business logic
       │   └─> telethon_core.py
       │
       ├─> utils/           <- Utilities
       │   └─> excel_generator.py
       │
       ├─> database.py      <- Data layer
       ├─> keyboards.py     <- UI components
       └─> config.py        <- Configuration
```

### Принципы

1. **Разделение ответственности**: Каждый модуль отвечает за свою часть
2. **Асинхронность**: Используем async/await везде где возможно
3. **Обработка ошибок**: Graceful degradation
4. **Логирование**: Все важные события логируются
5. **Конфигурация**: Все настройки через .env

## Коммуникация

### Где обсудить

- **Issues**: Баги, предложения
- **Pull Requests**: Обсуждение кода
- **Telegram**: Быстрые вопросы (см. SUPPORT_LINK)

### Код поведения

- Будьте уважительны
- Конструктивная критика приветствуется
- Помогайте новичкам
- Фокус на решении проблем

## Лицензия

Внося вклад, вы соглашаетесь с тем, что ваш вклад будет лицензирован под MIT License.

## Вопросы?

Не стесняйтесь задавать вопросы в Issues или Telegram.

---

**Спасибо за ваш вклад в NeuroScraper Pro! 🚀**
